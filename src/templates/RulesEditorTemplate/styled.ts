import styled from 'styled-components'

const SidebarContainer = styled.div`
  position: fixed;
  left: 0;
  z-index: 999;
  width: 240px;
  height: 100vh;
  background: #f0f0f0;
  user-select: none;
`

export const Styled = {
  SidebarContainer,
}
