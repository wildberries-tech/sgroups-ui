import styled from 'styled-components'

const SelectedItemsText = styled.div`
  display: flex;
  align-items: center;
  margin-right: 8px;
  font-size: 16px;
  line-height: 24px;
`

export const Styled = {
  SelectedItemsText,
}
